[project]
name = "gsheets-automations"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi",
  "uvicorn",
  "python-multipart",
  "pandas",
  "openpyxl",
  "xlrd",
  "reportlab",
]
